streamlit>=1.35
anthropic>=0.40
python-dotenv>=1.0
pandas>=2.2
openpyxl>=3.1
qdrant-client>=1.9

# Diagramme im Dashboard. Kommt mit Streamlit mit, steht hier aber
# ausdrücklich, weil ln_dashboard.py es direkt verwendet.
altair>=5.0

# Bild-Export: PNG aller Dashboard-Grafiken.
# Pillow kommt ebenfalls mit Streamlit, wird aber direkt benutzt.
vl-convert-python>=1.3
pillow>=10.0
