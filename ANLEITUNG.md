# LN Rechnungserfassung (Produkt 2)

Lokal starten:
  cd in diesen Ordner
  python3 -m venv .venv && source .venv/bin/activate
  pip install -r requirements.txt
  python3 -m streamlit run ln_billing_app.py

Online: eigenes GitHub-Repo, auf share.streamlit.io als neue App deployen
(Main file: ln_billing_app.py), Secrets wie in secrets_beispiel.toml.
